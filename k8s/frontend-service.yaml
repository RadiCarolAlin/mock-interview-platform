apiVersion: v1
kind: Service
metadata:
  name: mock-interview-frontend
  namespace: mock-interview
  labels:
    app: mock-interview-frontend
spec:
  type: ClusterIP
  selector:
    app: mock-interview-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80